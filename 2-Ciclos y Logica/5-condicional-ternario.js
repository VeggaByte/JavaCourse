let mensaje

const edad = 20;

if(edad >= 18){
    mensaje = 'Es mayor de edad, puede pasar'
}else{
    console.log = "No es mayor de edad, no puede pasar"
}

console.log(mensaje)

//Operador Ternario

// estructura->      condicion ? true : false

const edad2 = 20
const mensaje2 = edad >= 18 ? "es mayor de edad" : "es menor de edad no puede pasar"
console.log(mensaje)
