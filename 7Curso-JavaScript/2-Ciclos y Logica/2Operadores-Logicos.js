// Operadores Logicos

// && (AND) si este y este se cunmplen es true
// || (OR) si este o este se cumplen true
// ! (not) si este valor no es esto true


const a = 10
const b = 20
const c = "10"

a == b && a === c // false
a != b || a === c   // tur porque se cumple la izquierda porque la derecha no es mimso dato pero es or
!(a === c) // Regresa True porque el simbolo “!” se lee como NO, entonces sería “a no es igual a c” y eso es verdadero.